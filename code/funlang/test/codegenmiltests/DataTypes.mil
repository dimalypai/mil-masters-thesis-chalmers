type T
  = MkT (Pure_M (Int -> Pure_M Unit))
  | MkTT;

type Pair A B
  = MkPair A B;

alias Pure_M = Error Unit ::: NonTerm;

alias IO_M = Error Unit ::: NonTerm ::: IO;

main : IO_M Unit =
  let (var_0 : Unit) <- return [IO_M] unit in
    return [IO_M] var_0;

