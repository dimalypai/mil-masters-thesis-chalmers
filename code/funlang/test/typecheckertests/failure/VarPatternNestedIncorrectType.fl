main : IO Unit
main = do
  return unit;
end;;

type Pair A B = MkPair A B

caseExpr : Pair Int Int -> Int
caseExpr = \(pair : Pair Int Int) ->
  case pair of
    | MkPair (x : Int) (y : Float) => x
  end;;

