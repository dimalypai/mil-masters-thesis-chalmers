main : IO Unit
main = do
  return unit;
end;;

type Pair A B = MkPair A B

caseExpr : Pair Int Float -> Int
caseExpr = \(pair : Pair Int Float) ->
  case pair of
    | MkPair (x : Int) (x : Float) => x
  end;;

